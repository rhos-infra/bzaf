=============
bzaf Backends
=============

Backends in ``bzaf`` are used to execute user's request.

Each backend represents a different way of executing user defined logic.

Developers can develop custom backends for ``bzaf``, refer to
:ref:`developing_backends:Developing Backends`.

.. toctree::
   :maxdepth: 1
   :caption: Available backends

   shell_backend
   ansible_backend